<h1>Burgers</h1>

<h2>Ready to eat:</h2>

<ul>
    {{#each burgers}}
        {{#unless eaten}}
            {{> burgers/burger-block eaten=true}}
        {{/unless}}
    {{/each}}
</ul>

<h2>Already eaten:</h2>

<ul>
    {{#each burgers}}
        {{#if eaten}}
            {{> burgers/burger-block eaten=false}}
        {{/unless}}
    {{/each}}
</ul>

<h2>Order a burger:</h2>
<form class="create-form">
    <div class="form-group">
        <input type="text" id="burger-btn" name="burger">
    </div>
    <button type="submit">Order Burger</button>
</form>